mvn clean install
    -> jar를 만듬

mvn spring-boot:run

SpringBootApplication에서 어떤 application.properties을 사용할지 결정
    application-jenkins.properties
        -Dspring.profiles.active=jenkins
        spring.datasource.username = ${OPENSHIFT_MYSQL_DB_USERNAME}

docker build -t dnbnsvr .

docker run --restart unless-stopped -d -p 8089:8089 --name dnbnsvr -t dnbnsvr
